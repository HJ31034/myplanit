<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planit.mapper.main.UserMapper">

	<!-- 회원가입시 입력 내용 -->
	<insert id="saveUser">
		INSERT INTO
		USERS(USERID, PASSWORD, USERNAME, EMAIL,
		PHONE, REGDATE, USERAUTH)
		VALUES(#{userId}, #{password}, #{userName}, #{email}, #{phone}, SYSDATE, #{userAuth})

	</insert>


	<!-- 로그인 without security -->
	<select id="loginUser" resultType="UserDTO">
		SELECT USERNAME, USERID, EMAIL, PHONE
		FROM USERS
		WHERE USERID=#{userId}
		AND PASSWORD=#{password}


	</select>


	<!-- login with security -->
	<select id="findByUsername" resultType="UserDTO">
		SELECT * FROM USERS
		WHERE USERID = #{userId} 
	</select>

	<!-- 아이디 중복체크 -->
	<select id="idCheck" resultType="int">
		SELECT count(USERID)
		FROM USERS u
		WHERE USERID = #{userId}
	</select>

	

	<!-- 회원정보 보기 -->
	<select id="userInfo" resultType="UserDTO">
		SELECT USERID, USERNAME, EMAIL, PHONE
		FROM USERS
		WHERE USERID = #{userId, jdbcType=VARCHAR}

	</select>

	<!-- 비밀번호 변경 -->
	<update id="pwdCh">
		UPDATE USERS
		SET PASSWORD = #{password}
		WHERE USERID = #{userId}
	</update>

	<update id="infoCh">
		UPDATE USERS
		SET USERNAME=#{userName}, EMAIL=#{email}, PHONE=#{phone}
		WHERE USERID=#{userId}
	</update>

</mapper>