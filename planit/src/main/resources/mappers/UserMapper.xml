<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planit.mapper.main.UserMapper">

<!-- 회원가입시 입력 내용 -->
	<insert id="saveUser">
		INSERT INTO
		USERS(USERID, PASSWORD, USERNAME, EMAIL, PHONE)
		VALUES(#{userId}, #{password}, #{userName}, #{email}, #{phone})

	</insert>


	<!-- 로그인 -->
	<select id="loginUser" resultType="UserDTO">
	SELECT USERNAME, USERID, EMAIL, PHONE
	FROM USERS 
	WHERE USERID=#{userId}
	AND PASSWORD=#{password}


	</select>
	

	
	<!-- 아이디 중복체크 -->
	<select id= "idCheck" resultType="int" >
	SELECT count(USERID)
	FROM USERS u 
	WHERE USERID = #{userId}
	</select>
	
	<!-- 현재 비밀번호검사  -->
	<select id= "pwdCheck" resultType="int" parameterType="map" >
	SELECT count(password)
	FROM USERS u 
	WHERE USERID = #{userId} and password = #{password}
	</select>
	
	<!-- 회원정보 보기 -->
	<select id="userInfo" resultType="UserDTO">
	 SELECT USERID, USERNAME, EMAIL, PHONE
	FROM USERS
	WHERE USERID = #{userId} 
	
	</select>
	
	<!-- 비밀번호 변경  -->
	<update id="pwdCh"> 
	UPDATE USERS
	SET PASSWORD = #{password}
	WHERE USERID = #{userId}
	</update>
	
	<update id="infoCh" >
	UPDATE USERS
	SET USERNAME=#{userName}, EMAIL=#{email}, PHONE=#{phone}
	WHERE USERID=#{userId}
	</update>

</mapper>